{
  "rabbitmq": {
    "ssl": {
          "cert_chain_file": "/etc/pki/{{ grains['fqdn'] }}-ca/certs/{{ grains['fqdn'] }}.crt",
          "private_key_file": "/etc/pki/{{ grains['fqdn'] }}-ca/certs/{{ grains['fqdn'] }}.key"
        },
    "host": "localhost",
    "port": 5672,
    "user": "sensu",
    "password": "mypass",
    "vhost": "/sensu"
  },
  "redis": {
    "host": "localhost",
    "port": 6379
  },
  "api": {
    "host": "localhost",
    "port": 4567
  },
  "client": {
      "name": "{{ grains['fqdn'] }}-Monitor",
      "address": "localhost",
      "subscriptions": [ "ALL" ]
    }
}
